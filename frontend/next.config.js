/** @type {import('next').NextConfig} */
const nextConfig = {
  /* 여기에 원래 넣고자 했던 옵션들을 적습니다 */
  reactStrictMode: true,
  swcMinify: true,
  // 예: images: { domains: ["example.com"] },
};

module.exports = nextConfig;